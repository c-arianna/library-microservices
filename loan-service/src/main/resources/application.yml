server:
  port: 8083

spring:
  application:
    name: loan-service

  datasource:
    url: jdbc:mysql://localhost:3306/loan_db
    username: libraryApp
    password: libraryAppTest!

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate.type.json_format_mapper: mentoring.acomi.loanservice.infrastructure.persistence.configuration.Jackson3HibernateJsonFormatMapper

  security:
    user:
      name: none
      password: none

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest